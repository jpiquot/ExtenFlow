apiVersion: v1
kind: Service
metadata:
  name: {{ template "extenflowidentitystoreactors.fullname" . }}
  labels:
    app: {{ template "extenflowidentitystoreactors.name" . }}
    chart: {{ template "extenflowidentitystoreactors.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "extenflowidentitystoreactors.name" . }}
    release: {{ .Release.Name }}
